# ══════════════════════════════════════════════════════════════════════════════
#  requirements.docker.txt — CPU-safe Docker requirements
#
#  This file is used ONLY inside the Docker container (CPU mode).
#  It replaces onnxruntime-vitisai (Windows / NPU-only) with plain onnxruntime
#  and drops Windows-only packages (pywin32, lemonade-sdk, amd-quark).
#
#  For native Windows + NPU: use requirements.txt instead.
# ══════════════════════════════════════════════════════════════════════════════

# UI
nicegui>=1.4.0,<2.0.0

# ONNX Runtime — CPU only (replaces onnxruntime-vitisai in container)
onnxruntime>=1.17.0

# HTTP client (Ollama)
httpx>=0.25.0

# Vision
Pillow>=10.0.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0

# Screen capture (Linux-compatible subset)
mss>=9.0.0

# NLP
sentencepiece>=0.1.99
transformers>=4.40.0,<5.0.0

# System monitoring
psutil>=5.9.0

# Utilities
python-dateutil>=2.8.2
rich>=13.0.0
click>=8.1.0

# Document processing
pypdf>=4.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
